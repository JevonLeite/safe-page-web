import styled from 'styled-components'
import { ScaleLoader } from 'react-spinners'

export const LoadingAnimation = styled(ScaleLoader)`
  flex: 1;
  display: flex;
  justify-content: center;
  align-items: center;
`
